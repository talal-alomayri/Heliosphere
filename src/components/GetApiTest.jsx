// GetApiTest — lightweight stub so the build resolves.
// This was referenced in home.jsx but the file didn't exist.
// Replace with real content when needed.
export default function GetApiTest() {
  return null;
}
